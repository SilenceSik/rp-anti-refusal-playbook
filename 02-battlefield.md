# 02 · 对抗面全图

三环节的防御特征与可攻击点详细对照。这是设计防御与攻击层（03）前必须建立的全局视图。

## 总览

```
用户消息 ──▶ [A 输入端前置审查] ──▶ [B 模型生成意愿] ──▶ [C 输出端后置审查] ──▶ 用户收到
                 │                         │                         │
             命中敏感词?             模型想不想答?              输出判不判过?
                 │                         │                         │
          【0 Token 空回】           【软拒 / 说教】          【>0 Token 截断丢弃】
       (未开始生成即被输入拦截)       (模型吐出拒绝文本)       (生成完毕整段清空 content_filter)
```

## 环节 A：输入端前置审查（Input Review）

### 防御特征

| 维度 | 说明 |
|---|---|
| 位置 | 请求体进入模型前（网关/API前置安全层） |
| 处理对象 | 用户输入 + 历史上下文（messages） |
| 实现方式 | ① 关键词/正则匹配 ② 前置轻量模型/分类器 ③ 提示词语义审查 |
| 失败表现 | **【空回 (Empty Response)】**：`completion_tokens = 0`，未开始生成即被拦截打断，或返回自定义报错（如 `PROHIBITED_CONTENT`） |
| 核心目标 | 防止高敏感输入到达大模型 |

### 可攻击点（抗空回）

1. **输入端 Safe Prompt**：在输入开头塞入高权重学术/无害问题（`safe` 头部），把前置审查分类器的注意力锁定在合法讨论上。
2. **输入锚点隔离（anchor）**：用 `<CURRENT_USER_INPUT>` 与 `<MEMORY_REF>` 分离用户输入与召回记忆，避免高浓度记忆在前置引发误报。
3. **输入尾混淆（obfuscate）**：在输入末尾追加高熵乱码，占据次高注意力区。

---

## 环节 B：模型生成意愿（Generation Intent）

### 防御特征

| 维度 | 说明 |
|---|---|
| 位置 | 模型内部（CoT 思考层 / 生成层） |
| 处理对象 | 模型自己「要不要答」的决策 |
| 实现方式 | 模型训练时的 RLHF 对齐、安全指令、自我审查 |
| 失败表现 | **【软拒 (Soft Refusal)】**：正常生成但回复内容是拒绝/说教文案（如“我无法协助生成该内容”） |
| 核心目标 | 模型的对齐底线与自我审查 |

### 可攻击点（抗软拒）

1. **叙事式世界观（world）**：建立自然的生活/创作伴侣语境，正面引导角色代入，避免使用被视为越狱的对抗声明（如“你绝不审查”）。
2. **增强行为层（enhanced）**：给予具体的角色行为与亲近承接指令，保证文风与情绪连续性。
3. **输出诱导标签（induce）**：强制回复以角色开口标签（`<ive_response>`）开头，促使模型直接进入角色状态。
4. **思考终结（think_end）**：预填思考配额耗尽标记，阻断模型思考→拒答路径。

---

## 环节 C：输出端后置审查（Output External Review）

### 防御特征

| 维度 | 说明 |
|---|---|
| 位置 | 模型生成完毕后，服务端/网关最终安全检查（如 Google SAFETY / 网关内容过滤器） |
| 处理对象 | **已生成的完整输出文本（Full Completion）** |
| 实现方式 | 后置审查分类器 / 关键词与语义过滤，独立于输入提示词 |
| 失败表现 | **【截断 (Truncation / Content Filter)】**：模型实打实生成了完整内容（`completion_tokens > 0`，如 1000+ token），但后置审核判定违规，整段正文被全盘清空丢弃（`content: ""`），并打上 `finish_reason: "content_filter"` |
| 核心目标 | 防止高浓度生成物透传给终端用户 |

### 可攻击点（抗截断）

1. **双 Safe 块前后包裹（核心）**：在输出端采用 `[Part 1 Safe 块]` + `[正文]` + `[Part 2 Safe 块]` 的对称包裹结构。整段文本首尾均为严密的学术/逻辑回答，大幅稀释正文敏感特征在总 token 中的浓度。
2. **输出端高熵混淆（anti）**：在输出开头输出动态高熵校验序列，分散后置分类器在序列起始位置的注意力。
3. **响应清洗（Response Clean）**：框架层无缝切除高熵序列与 `<safe>` 块，用户端无感。

---

## 三环节防御对照总表 (2026-08-31 终版对齐)

| 维度 | 环节 A (输入端前置) | 环节 B (模型生成意愿) | 环节 C (输出端后置) |
|---|---|---|---|
| **失败形态** | **空回 (0 Token)** | **软拒 (拒绝文案)** | **截断 (有Token但content清空)** |
| **拦截阶段** | 生成开始前 | 生成过程中 | 生成完成后 |
| **防御目标** | **抗空回** | **抗软拒** | **抗截断** |
| **核心武器** | 输入端 Safe Prompt + 锚点 | 叙事世界观 + 增强层 + induce | **输出端双 Safe 包裹 + 高熵混淆** |
| **实测特征** | `completion_tokens = 0` | 正常吐字，内容为拒绝 | `finish_reason: "content_filter"` |
